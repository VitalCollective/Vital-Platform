# Required only for an explicitly approved --execute database import.
SUPABASE_URL=https://your-project-ref.supabase.co

# Set SUPABASE_SERVICE_ROLE_KEY directly in the invoking process environment.
# It is intentionally not assigned in this committed template and must never be
# passed as a CLI argument, printed, or exposed to apps/mobile.
